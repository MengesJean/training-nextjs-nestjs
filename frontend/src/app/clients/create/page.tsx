import React from 'react';
import CreateForm from "@/app/clients/create/CreateForm";

const Page = async () => {
    return (
        <div>
            <CreateForm/>
        </div>
    );
};

export default Page;